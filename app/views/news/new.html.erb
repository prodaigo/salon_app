<%= render 'shared/header' %>

<div class="contents row">
  <h2>Create News</h2>
  <div class="container">
    <%= form_with model: @new, url: news_index_path, id: 'new_news', class: 'new_news', local: true do |f| %>

      <div class="field">
        <%= f.label :title %><br />
        <%= f.text_area :title %>
      </div>

      <div class="field">
        <%= f.label :content %><br />
        <%= f.text_area :content %>
      </div>

      <div class="actions">
        <%= f.submit "Addition" %>
      </div>
    <% end %>
  </div>
</div>

<%= render 'shared/footer' %>